import { SchedulerDirective } from './scheduler.directive';

describe('SchedulerDirective', () => {
  it('should create an instance', () => {
    const directive = new SchedulerDirective();
    expect(directive).toBeTruthy();
  });
});
